const endpoints = {
    AUTH: {
        "LOGIN": "/api-bearer-auth/v1/login",
        "PROFILE": "/user/getUser",
        "REFRESH": "/api-bearer-auth/v1/tokens/refresh"
    }
}

export default endpoints
